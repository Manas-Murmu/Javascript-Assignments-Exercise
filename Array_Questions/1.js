// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

var name = "Manas Ranjan Murmu"; //String
var isLoggedIn = true; //Boolean;
var age = null;
var score;

console.log(`Name is ${name} and the type is ${typeof name}`);
console.log(`isLoggedIn is ${isLoggedIn} and the type is ${typeof isLoggedIn}`);
console.log(`age is ${age} and the type is ${typeof age}`);
console.log(`score is ${score} and the type is ${typeof score}`);
